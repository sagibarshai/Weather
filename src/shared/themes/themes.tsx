import { Colors } from "../colors/colors";
import colors from "../colors/colors";
  const darkMode = {
    primary: "#1a2b55",
  };
  
  const themes = { ...colors, ...darkMode };
  
  export default themes;